## Runtime brand/site
BRAND_NAME=Raipur Cabs
SITE_DOMAIN=localhost:3000

## Prisma/DB
# Dev (SQLite – no root, file lives in repo)
DATABASE_URL="file:./cabdb.sqlite"

# --- Production (Postgres) ---
# Uncomment and set in production environment only
# DATABASE_URL="postgresql://user:password@host:5432/cabdb?schema=public"

# Google Maps Platform (optional)
MAPS_ENABLED=false
GOOGLE_MAPS_BROWSER_KEY=
GOOGLE_MAPS_SERVER_KEY=
MAPS_REGION=IN
MAPS_LANGUAGE=en

## Manage My Bookings feature
FEATURE_MY_BOOKINGS=false
NEXT_PUBLIC_FEATURE_MY_BOOKINGS=false
MB_SESSION_TTL_HOURS=24
MB_SESSION_SECRET=change-me
OTP_BYPASS=false
